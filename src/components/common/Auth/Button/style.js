import styled from "styled-components";

export const Button = styled.button`
  width: 388px;
  height: 60px;

  border: none;
  border-radius: 10px;
  background: #ffc300;

  color: #f5f5f5;

  font-family: Pretendard;
  font-size: 18px;
  font-style: normal;
  font-weight: 700;
  line-height: 20px;
  cursor: pointer;

  &:focus {
    outline: none;
  }
`;
